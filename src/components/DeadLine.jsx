export default function DeadLine(record, conf) {
    return(
        <div>
            <div className="bg-blue-500 text-white text-center py-2 text-2xl">
                {JSON.stringify(conf)}
            </div>
        </div>
    )
}
